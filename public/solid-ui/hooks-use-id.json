{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "hooks-use-id",
  "type": "registry:lib",
  "files": [
    {
      "type": "registry:lib",
      "target": "lib/hooks/use-id.ts",
      "content": "let uniqueId = 0\nconst getUniqueId = () => uniqueId++\nconst prefix = \"S_\"\n\nexport function useId() {\n  let ref\n  if (ref === undefined) {\n    ref = getUniqueId()\n  }\n  return `${prefix}${ref}`\n}\n",
      "path": "src/lib/hooks/use-id.ts"
    }
  ]
}
